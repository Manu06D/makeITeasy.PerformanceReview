{{> dbimports}}

namespace {{namespace}}
{
    public partial class {{class}} : IdentityDbContext<IdentityUser>
    {
    {{{> dbsets}}}
    {{#if entity-type-errors}}
    {{#each entity-type-errors}}
    {{{entity-type-error}}}
    {{/each}}

    {{/if}}

    {{{> dbconstructor}}}

    {{#unless suppress-on-configuring}}
    {{{> dbonconfiguring}}}

    {{/unless}}
    {{{on-model-creating}}}
    }
    }
